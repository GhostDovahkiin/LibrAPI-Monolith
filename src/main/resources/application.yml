spring:
    profiles:
        active: dev
    jpa:
        hibernate:
            ddl-auto: update
    flyway:
        baseline-on-migrate: false
        enabled: false
        locations: classpath:/db/migration

springdoc:
    api-docs:
        path: /v3/api-docs
    swagger-ui:
        operationsSorter: method